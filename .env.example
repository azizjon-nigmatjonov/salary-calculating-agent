# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Required — Telegram bot token from @BotFather
BOT_TOKEN=8990655359:AAH8V7gIlPP1FBCjMAhaXJGPnAYBZutPvMA

# Optional — Ollama model for intent + replies (default: llama3.2)
OLLAMA_MODEL=llama3.2

# Optional — Whisper model for voice (base | small | medium)
WHISPER_MODEL=small

# Optional — temp directory for voice files (default: system temp)
# AUDIO_TMP_DIR=

# Optional — conversation memory size per chat (default: 10)
# CONVERSATION_HISTORY_LIMIT=10
